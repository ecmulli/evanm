[build]
builder = "dockerfile"
dockerfilePath = "apps/zeroclaw/Dockerfile"
watchPatterns = ["apps/zeroclaw/**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
